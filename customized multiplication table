#include <stdio.h>

int main() {
    int i,n,prod;
    printf("Enter the num");
    scanf("%d",&n);
    for(i=1;i<=10;i++)
    {
        prod=i*n;
        printf("%d*%d=%d\n",i,n,prod);
    }

    return 0;
}

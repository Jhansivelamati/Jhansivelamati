#include<stdio.h>
#include<conio.h>
int fact(int);
int fact(int a)
{
    if(a==1)
        return;
    else
       return a*fact(a-1);
}
void main()
{
    int a;
    printf("Enter any integer:");
    scanf("%d",&a);
    printf("The factorial of %d is:%d",a,fact(a));
}
